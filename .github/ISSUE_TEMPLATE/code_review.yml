name: Code Review
description: A code review conducted with a Technical Leader (TL) or the course instructor.
title: "Code Review: Lab 2, Beacon on Pig Hill"
body:
  - type: markdown
    attributes:
      value: |
        Fill this out with the programmer present. Leave a box unchecked when the work does
        not meet it. An unchecked box is information, not a punishment.
  - type: input
    attributes:
      label: Reviewer
      description: The GitHub username of the TL or instructor conducting this review
      placeholder: your-github-username
    validations:
      required: true
  - type: checkboxes
    attributes:
      label: Functional requirements
      description: Check each of the following satisfied functional requirements
      options:
        - label: "Code solves the issue presented"
        - label: "Code functions, even if not fully solving the issue presented"
        - label: "Performance of the code is acceptable"
  - type: checkboxes
    attributes:
      label: Code style and legibility
      description: Check each of the following checkboxes that describe code legibility
      options:
        - label: The code contains comments
        - label: Variable names are accurate and appropriate
        - label: The code is easy to read
        - label: The code is formatted correctly
  - type: markdown
    attributes:
      value: |
        Conduct a short walkthrough with the programmer, marking the topics appropriately explained.
  - type: checkboxes
    attributes:
      label: Code walkthrough
      description: Ask each question below about the author's own code, with the program running on their Pico. Check the box when the answer covers the point in parentheses.
      options:
        - label: "Light the Tower. Run it with no. Which line turned the LED off, and which comparison chose that branch? (led.off() in the else, reached because switch_on == \"yes\" was False)"
        - label: "The Wind Gauge. If the 20 or more test came first, what would 180 print, and why? (STEADY, because Python runs the first branch that is True and skips the rest, so the strongest test must come first)"
        - label: "The Tree Line. If the or were an and, which of the four answer pairs would log differently? (the two pairs with one yes would log CLEAR instead of NOT ALONE, because and needs both)"
        - label: "The Ride Home. With no code and a battery at 100, why LEFT BEHIND and not PICKED UP? (the battery is only consulted once the code is known: the charge check is indented inside the code check, or and needs both to hold)"
        - label: "The author described their process well, communicating confidence and understanding of their own code"
  - type: textarea
    attributes:
      label: The Good, The Bad, The Ugly
      description: Write a short note with at least 3 aspects done well, and 3 aspects on which the code could be improved
      value: |
        #### Positives
        1.
        2.
        3.
        #### Improvements
        1.
        2.
        3.
    validations:
      required: true
